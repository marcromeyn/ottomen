language: python
python:
  # We don't actually use the Travis Python, but this keeps it organized.
  - "2.7"
  - "3.4"
install:
  # We do this conditionally because it saves us some downloading if the
  # version is the same.
  - if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
      wget https://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh;
    else
      wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
    fi
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  # Useful for debugging any issues with conda
  - conda info -a
  - conda create create -p $HOME/py --yes 'scipy numpy' "python=$TRAVIS_PYTHON_VERSION"
  - export PATH=$HOME/py/bin:$PATH
  - python setup.py install
addons:
  postgresql: "9.3"
services:
  - redis-server
env:
  - SQL_DBNAME=ottomen SQL_USER=docker SQL_PASS=docker TRAVIS=true
before_script:
  - psql -c 'create database ottomen;' -U postgres
  - python manage.py db upgrade
notifications:
  slack: mechanicalturkteam:E0PjYxU4NlXUfLF8i4zb0z99
  email: false
script:
  - py.test -v