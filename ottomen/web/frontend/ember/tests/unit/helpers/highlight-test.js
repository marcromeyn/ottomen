import { highlight } from '../../../helpers/highlight';
import { module, test } from 'qunit';

module('Unit | Helper | highlight');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = highlight(42);
  assert.ok(result);
});
