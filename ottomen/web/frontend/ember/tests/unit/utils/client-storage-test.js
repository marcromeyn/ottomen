import clientStorage from '../../../utils/client-storage';
import { module, test } from 'qunit';

module('Unit | Utility | client storage');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = clientStorage();
  assert.ok(result);
});
