import { moduleFor, test } from 'ember-qunit';

moduleFor('view:questions/index', 'Unit | View | questions/index');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
